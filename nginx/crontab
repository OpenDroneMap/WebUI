# Automatically renew the SSL certificate (if needed)
0 0 1 * * root source /webui/.cronenv; bash -c "/webui/nginx/letsencrypt-autogen.sh"

# Rotate logs
0 0 * * * root bash -c "logrotate -f /webui/nginx/logrotate-nginx"

# An empty line is required at the end of this file for a valid cron file.
