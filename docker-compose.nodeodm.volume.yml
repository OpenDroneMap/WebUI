# Chaining this file to the main docker-compose file setups
# volumes for NodeODM.

version: '2.1'
services:
  node-odm-1:
    volumes:
      - ${WO_NODE_DIR}/data:/var/www/data:Z
      - ${WO_NODE_DIR}/tmp:/var/www/tmp:Z
