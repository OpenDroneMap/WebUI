# This configuration adds the volumes necessary for SSL manual setup
version: '2.1'
services:
  webapp:
    volumes:
      - ${WO_SSL_KEY}:/webui/nginx/ssl/key.pem
      - ${WO_SSL_CERT}:/webui/nginx/ssl/cert.pem
